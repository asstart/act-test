name: Publish

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "test"
        uses: asstart/obsidian-publish-jsaction@39dbc336fda0ca73d4ed0e69b74ee4d68287481d
        with:
          token: ${{ secrets.OBISDIAN_PUBLISH_TOKEN }}
          repor: asstart/vault
