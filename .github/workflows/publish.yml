name: Publish

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "test"
        uses: asstart/obsidian-publish-action@c6df49e989cbc971a8704eedc0a87c932c3e3fb7
        with:
          token: ${{ secrets.OBISDIAN_PUBLISH_TOKEN }}
