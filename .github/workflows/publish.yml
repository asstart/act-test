name: Publish

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "test"
        uses: asstart/obsidian-publish-action@4b8a175a61df055db387807d0a63970ae46d29f9
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
