name: Publish

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "test"
        uses: asstart/obsidian-publish-action@afe996884f82724871eeb5eca972464bafc22cbb
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
