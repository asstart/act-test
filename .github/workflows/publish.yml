name: Publish

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "test"
        uses: asstart/obsidian-publish-action@97d5d984c258f934a2f49135dea953b1652e998c
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
