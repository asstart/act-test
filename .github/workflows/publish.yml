name: Publish

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "test"
        uses: asstart/obsidian-publish-action@9090bfbbb43774dfc67c3aff7a83ff3e3c07c0a9
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
