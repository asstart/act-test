name: Publish

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "test"
        uses: asstart/obsidian-publish-action@a9ca996c0416312ba2d62a43e25ea5d5e9005e47
        with:
          token: ${{ secrets.OBISDIAN_PUBLISH_TOKEN }}
