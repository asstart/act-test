name: Publish

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "test"
        uses: asstart/obsidian-publish-action@fd58fec7a20d18f598f8ce94ca48c5f286c64356
        with:
          token: ${{ secrets.OBISDIAN_PUBLISH_TOKEN }}
