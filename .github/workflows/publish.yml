name: Publish

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "test"
        uses: asstart/obsidian-publish-action@352a18cda25dcb862563b4e2b9f2107b1c6f1b1d
        with:
          token: ${{ secrets.OBISDIAN_PUBLISH_TOKEN }}
