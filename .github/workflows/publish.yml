name: Publish

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "test"
        uses: asstart/obsidian-publish-action@3015c5c05326a261cceb90d3394994a8cd241705
        with:
          token: ${{ secrets.OBISDIAN_PUBLISH_TOKEN }}
