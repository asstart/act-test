name: Publish

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "test"
        uses: asstart/obsidian-publish-action@88d8d10491fae4a8024db8c2456f915a090c0b18
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
