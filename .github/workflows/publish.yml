name: Publish

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "test"
        uses: asstart/obsidian-publish-action@021da3dc0c3ebe8a42d4ce4fdc8ab2e30c3bb1f6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
