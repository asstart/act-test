name: Publish

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "test"
        uses: asstart/obsidian-publish-action@408e9a405bc77e75ca28272989fd772abdc97b61
        with:
          token: ${{ secrets.OBISDIAN_PUBLISH_TOKEN }}
